# Stuff in the Repo
archive/
legacy/
*.txt

# Home Directories
Applications
Desktop
Documents
Downloads
Dropbox
Google Drive
Library
Movies
Music
Pictures
Public
Sites
VirtualBox VMs
work

# Misc files
**/.DS_Store
**/.Trash

# Config
.config/zsh/.zcompdump
.config/chezmoi                   # Chezmoi's own state
.config/hub                       # GitHub credentials
.config/iterm2                    # Syncs separately or machine-specific
.config/nix/result                # Nix build output (symlink)
.config/zed                       # Zed syncs via account

##############################################
# Ignore (machine-local, generated, or secrets)
##############################################

.android                          # SDK/emulator state
.aws                              # Credentials - don't commit
.aws-sso                          # Credentials - don't commit
.cache                            # Cache
.claude/*                         # Local AI state
!.claude/CLAUDE.md                # Track global claude instructions
.claude.json*                     # Local AI state
.cups                             # Printer state
.docker                           # Docker credentials/state
.dropbox                          # Dropbox state
.fly                              # Fly.io credentials
.keychain                         # SSH agent helper state
.local/share                      # XDG runtime data
.local/state                      # XDG runtime data
.lpass                            # LastPass credentials
.mitmproxy                        # Certs/state
.nix-defexpr                      # Nix manages these
.nix-profile                      # Nix manages these
.pgadmin                          # DB tool state
.ssh                              # Never commit private keys
.vscode                           # Syncs via account
.zsh_history                      # History
