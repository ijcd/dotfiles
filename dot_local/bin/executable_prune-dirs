#!/bin/sh
# prune-dirs - Remove empty directories recursively
#
# Usage: prune-dirs <path>...
#
# Finds and removes all empty directories under the given paths,
# working from the deepest level up. Safe operation - only removes
# directories that are completely empty.
#
# Examples:
#   prune-dirs .              # Clean empty dirs in current directory
#   prune-dirs ~/Downloads    # Clean empty dirs in Downloads
#   prune-dirs dir1 dir2      # Clean multiple directory trees
#
# Use case: After moving/deleting files, clean up the empty
# directory structure left behind.

set -e

if [ $# -eq 0 ]; then
  echo "Usage: prune-dirs <path>..." >&2
  exit 1
fi

find "$@" -type d -empty -depth -delete
