{{- /* Per-machine default identity for ~/work (your machine-local source dir).
       Rendered by chezmoi from the hostname. ~/work is primarily Lunar work on
       the work laptop, personal everywhere else. Add work machines to the test
       below as needed. Specific subtrees (e.g. ~/work/lunar) are pinned in the
       main config and override this default. */ -}}
# Generated by chezmoi (source: dot_config/git/git-machine.tmpl) for {{ .chezmoi.hostname }}.
[includeIf "gitdir:~/work/"]
	path = {{ if eq .chezmoi.hostname "blackbird" }}git-work-lunar{{ else }}git-ijcd{{ end }}
